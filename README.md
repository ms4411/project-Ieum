# project-Ieum

## 최초 1회

```
git pull origin main --allow-unrelated-histories 
git remote remove origin
git remote add origin 저장소주소
```

## 커밋 및 푸시

```
git add . (수정된 모든 파일 추가)
git commit -m "커밋 메시지" (작업 내용 요약)
git push -u origin main (기본 브랜치로 업로드)
```
